# Path to the SQLite database file.
# On Railway: set to /data/creature-hunt.sqlite (persistent volume mounted at /data)
DATABASE_PATH=/data/creature-hunt.sqlite

# Port (Railway sets this automatically)
PORT=3001

# Optional: frontend origin for CORS (e.g. https://your-app.vercel.app). If unset, all origins are allowed.
# FRONTEND_ORIGIN=https://your-app.vercel.app
